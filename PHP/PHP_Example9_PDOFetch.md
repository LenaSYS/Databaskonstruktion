### Overview

This example shows how to use prepared statements and selects combined with the fetch statement.


The main difference compared to the previous example is that we use a normal form, and not a link, and therefore use POST instead of GET.
