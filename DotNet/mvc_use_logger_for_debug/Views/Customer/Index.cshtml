@using System.Data
@model DataTable
@{
    ViewBag.Title = "Customer Area";
}
<table>
    <tr>
        @foreach (DataColumn dataColumn in Model.Columns)
        {
            string columnName = char.ToUpper(dataColumn.ColumnName[0]) + dataColumn.ColumnName.Substring(1);
            <th>@columnName</th>
        }
    </tr>
    @foreach (DataRow CustomerRow in Model.Rows)
    {
        <tr>
            @for (int i = 0; i < Model.Columns.Count; ++i)
            {
                <td>@CustomerRow[i]</td>
            }
        </tr>
    }
</table>
@Html.ActionLink("Link back to home", "Index", "Home")
<!-- Position of the section is irrelevant -->
@section PageHeader {
    <h2>Customer Area - List of all customers</h2>
}